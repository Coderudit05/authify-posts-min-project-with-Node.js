<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <script src="https://cdn.tailwindcss.com"></script>
  <title>Edit Post</title>
</head>
<body class="bg-black text-white flex items-center justify-center min-h-screen p-4">
  <div class="bg-zinc-900 p-6 rounded-lg w-full max-w-lg">
    <h1 class="text-2xl font-bold mb-4">Edit your post</h1>

    <form action="/post/<%= post._id %>/edit" method="post" class="flex flex-col gap-3">
      <textarea name="content" rows="5" class="bg-gray-800 text-white p-3 rounded" required><%= post.content %></textarea>
      <div class="flex gap-2">
        <button type="submit" class="bg-blue-500 px-4 py-2 rounded">Save</button>
        <a href="/dashboard" class="bg-gray-600 px-4 py-2 rounded">Cancel</a>
      </div>
    </form>
  </div>
</body>
</html>
